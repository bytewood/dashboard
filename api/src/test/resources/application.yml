spring:
  jpa:
    database: H2
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  main:
    allow-bean-definition-overriding: true

logging:
  level:
    bison: DEBUG
    org.springframework.jdbc: INFO
